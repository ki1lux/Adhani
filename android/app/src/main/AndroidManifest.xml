<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ──────────────────────────────────────────────────
         Every permission below is exercised by code in this app. Anything
         that wasn't (ACCESS_BACKGROUND_LOCATION, USE_FULL_SCREEN_INTENT,
         ACCESS_NOTIFICATION_POLICY) has been removed: Play requires a
         declaration + justification for each of those, and an unused
         permission is a rejection waiting to happen.
    -->

    <!-- Location: prayer times and the Qibla bearing are both computed from
         the user's coordinates. Foreground use only — the app never reads
         location while backgrounded. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Aladhan API (prayer times) + Nominatim (place names). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Alarms: the Adhan must sound at the exact minute, so an inexact
         alarm is not a substitute. The app degrades to an inexact alarm
         when the user declines this (see AlarmSchedulerHelper). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 13+ runtime notification permission. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground services: Adhan playback (mediaPlayback) and the live
         countdown notification (specialUse — declared in Play Console). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- ── Hardware ─────────────────────────────────────────────────────
         All optional. A device with no compass still gets prayer times,
         alarms and the widget — QiblaScreen already renders an explanatory
         state for it — so requiring the sensor would exclude those users
         from the Play listing for no reason.
    -->
    <uses-feature
        android:name="android.hardware.sensor.compass"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:roundIcon="@mipmap/launcher_icon"
        android:supportsRtl="true"
        android:allowBackup="false">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot Receiver — re-schedules prayer alarms after reboot or app
             update. Must be exported: the system is the sender. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Prayer triggers are absolute epoch instants derived from a local
             HH:mm, so a timezone or clock change invalidates all five without
             making any of them look invalid. Must be exported: the system is
             the sender. Neither action requires a permission. -->
        <receiver
            android:name=".TimeChangeReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>

        <!-- Fired by AlarmManager at each prayer time. Only this app ever
             sends to it, so it stays unexported. -->
        <receiver
            android:name=".PrayerAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- ── Foreground services ──────────────────────────────────────
             AdhanAlarmService plays the Adhan on the alarm stream, so
             mediaPlayback is the type that describes what it does.
        -->
        <service
            android:name=".AdhanAlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- The live countdown to the next prayer. No standard FGS type
             covers "a user-visible countdown that must stay accurate while
             the app is closed", hence specialUse. The subtype below is what
             the Play Console declaration must match. The user can turn this
             notification off entirely in Settings. -->
        <service
            android:name=".PrayerCountdownService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Live countdown to the next prayer time, shown as an ongoing notification the user can disable in the app's settings." />
        </service>

        <!-- geolocator declares this service with
             android:foregroundServiceType="location", which the manifest
             merger pulls into ours. This app never uses foreground location:
             it calls getCurrentPosition/getLastKnownPosition while in use and
             passes no foregroundNotificationConfig, so the service is only
             ever bound, never promoted with startForeground(). Leaving the
             type in place advertises a location foreground service that we
             deliberately hold no FOREGROUND_SERVICE_LOCATION permission for —
             an inconsistency a Play reviewer or an automated manifest scan
             reads as undeclared background-location use. Stripping the
             attribute (not the service — geolocator binds to it, so removing
             the node would break location entirely) makes the manifest say
             what the app actually does. -->
        <service
            android:name="com.baseflow.geolocator.GeolocatorLocationService"
            tools:remove="android:foregroundServiceType" />

        <!-- Home Screen Widget. Exported because the launcher binds it. -->
        <receiver
            android:name=".PrayerWidgetProvider"
            android:exported="true"
            android:label="@string/widget_label">
            <!-- APPWIDGET_UPDATE only. ACTION_PRAYER_UPDATED used to be
                 listed here too, which meant any installed app could
                 broadcast it and drive this receiver — the one third-party
                 reachable surface in the app. Every sender of it is now
                 component-addressed (AlarmSchedulerHelper, PrayerUpdateWorker
                 and the per-prayer widget-refresh PendingIntents), and an
                 explicit intent is delivered without needing a filter to
                 match, so nothing is lost by not advertising it. -->
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info" />
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Package visibility (Android 11+): opening the developer's GitHub
         page and the Play listing needs a browser/Play resolver. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
